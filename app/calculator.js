const add = (x,y) => {
  return x+y;
}

const multiply = (x,y,) => {
  return x*y;
}

const minus = (x,y) => {
  return x-y;
}

export { add, multiply, minus};
export default add; // makes add function as default funciton
